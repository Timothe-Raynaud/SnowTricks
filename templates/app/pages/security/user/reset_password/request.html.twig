{% extends 'app/layout/_layout.html.twig' %}

{% block title %}{{ siteName }} - Reset your password{% endblock %}

{% block content_attributes %}content-security{% endblock %}

{% block main %}
    <h3 class="text-center">Mot de passe oubliée</h3>

    <div class="mt-4 mb-2">
        <small>
            Veuillez renseigner l'adresse email de votre compte :
        </small>
    </div>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}

    <div class="text-center mt-3">
        <button class="button button-secondary">Envoyer</button>
    </div>
    {{ form_end(requestForm) }}
{% endblock %}
